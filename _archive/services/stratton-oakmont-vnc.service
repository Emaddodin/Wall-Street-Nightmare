[Unit]
Description=VNC into the chart display, loopback only
After=stratton-oakmont-xvfb.service
Requires=stratton-oakmont-xvfb.service

[Service]
User=stratton
Environment=DISPLAY=:99
# -localhost is the whole security model here: the only way in is an SSH
# tunnel. An open VNC port would hand anyone the logged-in TradingView
# session and, through it, the account.
ExecStart=/usr/bin/x11vnc -display :99 -localhost -forever -shared -rfbauth /home/stratton-oakmont/.vnc/passwd -quiet
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
