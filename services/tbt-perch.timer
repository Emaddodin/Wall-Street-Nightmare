[Unit]
Description=Re-perch every two minutes

[Timer]
# Two minutes, matching the guard's own round: there is no point naming a coin
# faster than the guard can move the chart onto it.
OnBootSec=4min
OnUnitActiveSec=2min
AccuracySec=15s

[Install]
WantedBy=timers.target
