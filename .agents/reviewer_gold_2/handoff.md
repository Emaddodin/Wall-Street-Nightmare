# Comprehensive Review & Adversarial Audit Report: LLM Intuition, Macro Blackout & Watchdog Suite

**Reviewer**: `reviewer_gold_2` (Roles: reviewer, critic)  
**Parent**: `orchestrator_2` (convId: `d8cde56b-142d-4ad0-b360-6f180e2c8eaa`)  
**Timestamp**: 2026-09-17T19:46:00Z  
**Verdict**: **APPROVE**  
**Integrity Audit**: **CLEAN (Zero Integrity Violations)**  

---

## 1. Observation

Direct code and test observations from inspected modules:

### 1.1 Local llama.cpp Exit Intuition & GBNF Grammar (`macro/slm_intuition.py`)
- **GBNF Grammar (lines 250–253)**:
  ```python
  GBNF_INTUITION_GRAMMAR = r'''
  root ::= "{" ws "\"decision\":" ws ("\"HOLD\"" | "\"EXIT\"") ws "}"
  ws ::= [ \t\n\r]*
  '''.strip()
  ```
  Strictly restricts output tokens generated by `llama-server` to either `{"decision": "HOLD"}` or `{"decision": "EXIT"}`.
- **Latency Ceiling & Fail-Safe Fallback (lines 267, 328–335, 371–390, 401–408)**:
  `timeout_sec = 0.300` (300ms ceiling). In `query_intuition_exit`:
  Catches `asyncio.TimeoutError`, non-200 HTTP response codes, and general network exceptions, invoking `_fail_safe_evaluation(telemetry, latency_ms, reason)`.
  If `telemetry.unrealized_r >= 1.0 and telemetry.candle_wick_ratio >= 0.65 and telemetry.volume_stall`, returns `IntuitionDecision.EXIT`, otherwise falls back to `IntuitionDecision.HOLD`. Execution never stalls or blocks the event loop.

### 1.2 Macro Fundamental Economic Calendar Blackout (`macro/slm_intuition.py`)
- **Polling & Window (lines 104–105)**:
  `poll_interval_sec = 600` (10 minutes), `blackout_window_sec = 900` (15 minutes).
- **High-Impact Filter (lines 68–82, 129–141)**:
  Filters for `ev.currency.upper() == "USD"` and either `ev.impact.upper() == "HIGH"` or high-impact keyword match (`CPI`, `CONSUMER PRICE INDEX`, `NFP`, `NON-FARM`, `FOMC`, `FED INTEREST RATE`, `FEDERAL FUNDS RATE`, `POWELL`, `PPI`, `GDP`, `UNEMPLOYMENT RATE`).
  Evaluates `diff = now - ev.timestamp`: returns `True` if `-900 <= diff <= 900` (15m before and after release).

### 1.3 Systemd Services & Memory Quotas (`deploy/`)
- **`deploy/relapse-scalper.service` (lines 11–13)**:
  ```ini
  MemoryMax=600M
  CPUQuota=100%
  ExecStart=/usr/bin/python3 /root/ict_sniper/run_relapse_scalper.py --paper
  Restart=always
  RestartSec=5
  ```
- **`deploy/stratton-llm-critic.service` (lines 10–19)**:
  ```ini
  LimitMEMLOCK=infinity
  MemoryMax=1800M
  ExecStart=/root/ict_sniper/llama.cpp/llama-server \
      -m /root/ict_sniper/models/qwen2.5-coder-1.5b-instruct-q4_k_m.gguf \
      --host 127.0.0.1 \
      --port 8080 \
      -t 2 \
      -ngl 0 \
      --mlock \
      --ctx-size 2048
  ```
  Enforces sub-3B Qwen2.5-Coder-1.5B Q4_K_M GGUF model with 2 vCPUs (`-t 2`), CPU-only inference (`-ngl 0`), memory lock (`--mlock`), and context 2048.
  Total systemd cgroup budget: `600M + 1800M = 2400M` (< 2560 MB resident limit).
- **`deploy/relapse-watchdog.service` & `relapse-watchdog.timer`**:
  Configures oneshot health checks at 30-second cadence (`OnUnitActiveSec=30s`).
- **`deploy/install_llama.sh` (lines 14–16)**:
  Targets `qwen2.5-coder-1.5b-instruct-q4_k_m.gguf` from Hugging Face and prebuilt `llama-server` release `b11009`.

### 1.4 VPS Memory Guard & Alert Escalation (`quant/hft/guard.py`)
- **Resident RAM Calculation (lines 77–93)**:
  Accurately parses `/proc/meminfo` lines `MemTotal` and `MemAvailable`, returning `(total - avail) / 1024.0` in MB. Gracefully handles non-Linux / macOS environments returning 0.0 MB.
- **RAM Ceiling (line 34)**:
  `MAX_SYSTEM_RAM_MB = 2560.0` (2.5 GB).
- **Self-Healing & Supervised Units (lines 30–31, 213–246)**:
  Supervises `relapse-scalper.service` and `stratton-llm-critic.service`. Checks `/health` endpoint on `http://127.0.0.1:8080/health`. Detects stale telemetry (`age > 45s`). Triggers auto-restart via `systemctl restart <unit>`.
- **Antigravity JSON Telemetry (lines 39–63)**:
  Single-line serialized JSON with fields `timestamp`, `agent: "watchdog_guard"`, `component`, `event`, `level`, `data`.
- **ntfy.sh Escalation (lines 113–148, 278–300)**:
  Sends push alerts to `ntfy.sh/{topic}` with `urgent` priority and tags `rotating_light,fire,warning` on critical faults (service death, RAM ceiling breach).

### 1.5 Scalper CLI & Diagnostics (`run_relapse_scalper.py`)
- Supports `--calendar-url`, `--paper`, `--dry-run`, and `--initial-equity` flags.
- Dry-run successfully initializes broker venue, risk invariants, killzone guard, calendar filter, SLM intuition engine, and FSM, logs structured diagnostic telemetry, and exits with code 0.

### 1.6 Empirical Test Execution Results
- `pytest tests/test_hft_guard.py -v`:
  **14 passed in 4.50s** (Meminfo parsing, critic health 200 / failure, constants, inactive service auto-restart, RAM breach detection, telemetry schema, ntfy escalation, systemd file validation, stale telemetry).
- `python3 run_relapse_scalper.py --paper --dry-run --initial-equity 65.0`:
  **Exited with code 0**. Telemetry confirmed `DRY_RUN_DIAGNOSTIC_PASSED` across all 5 core components.
- `python3 run_relapse_scalper.py --paper --dry-run --initial-equity 65.0 --calendar-url "http://localhost:9999/calendar"`:
  **Exited with code 0**. Telemetry confirmed `--calendar-url` argument ingested and processed nominal.
- `pytest tests/test_gold_relapse_scalper.py tests/test_scaling_simulation.py -v`:
  **30 passed in 4.15s** (All CLOB invariants, order slicing, detached stops, breakeven locks, macro blackouts, SLM intuition mocked exits, daily drawdown killswitches, Monte Carlo compounding).

---

## 2. Logic Chain

1. **Requirement R2 (Sub-Second Intuition Exit & Schema)**:
   - Observation 1.1 shows `SLMIntuitionEngine` implements the GBNF grammar for llama.cpp strictly limiting outputs to `{"decision": "HOLD"}` and `{"decision": "EXIT"}`.
   - Timeout is set to `0.300s` with a timeout handler in `try/except asyncio.TimeoutError`.
   - On timeout or HTTP error, `_fail_safe_evaluation` triggers algorithmic capital preservation based on `unrealized_r`, `candle_wick_ratio`, and `volume_stall`.
   - Therefore, Requirement R2 is fully satisfied without blocking risks.

2. **Requirement R3 (Macro Fundamental Calendar Blackout)**:
   - Observation 1.2 shows `EconomicCalendarFilter` polls every 600s (10m) and calculates the absolute time delta against USD high-impact releases.
   - Blackout window `[-900s, +900s]` strictly blocks entries during the ±15m window.
   - FSM causally checks `self.calendar.is_macro_blackout(now_ts)` on every 5M bar before state transition.
   - Therefore, Requirement R3 is fully satisfied.

3. **Requirement R5 & Production Hardening**:
   - Observation 1.3 shows `stratton-llm-critic.service` executes `llama-server` with `qwen2.5-coder-1.5b-instruct-q4_k_m.gguf` under `MemoryMax=1800M`.
   - `relapse-scalper.service` enforces `MemoryMax=600M`.
   - Combined maximum cgroup RSS is `2400 MB`, which is strictly below the host ceiling of `2560 MB` (2.5 GB) on a 4GB VPS.
   - Observation 1.4 confirms `quant/hft/guard.py` continuously audits `MemTotal - MemAvailable`, supervises both services, emits Antigravity JSON telemetry, and escalates to ntfy.sh with urgent priority.
   - Therefore, Requirement R5 is fully satisfied.

4. **Integrity Verification**:
   - Zero hardcoded test return fixtures found in source code (`get_system_ram_used_mb` reads `/proc/meminfo`, `is_service_active` calls `systemctl`, `query_intuition_exit` uses `aiohttp`).
   - No facades or stubbed tasks.
   - Therefore, implementation integrity is certified CLEAN.

---

## 3. Caveats

- **External Calendar API**: When `--calendar-url` is omitted or unconfigured, the calendar monitor operates on manually registered events or local mock feeds. In production on the VPS, an active calendar JSON endpoint or webhook should be configured via `--calendar-url` or `.env`.
- **Operating System Environment**: On Darwin (macOS local development), `/proc/meminfo` does not exist natively; `quant/hft/guard.py` safely defaults to returning `0.0 MB` without crashing, while unit tests mock `/proc/meminfo` to verify Linux compatibility. In production on Ubuntu 24.04 (`/root/ict_sniper`), `/proc/meminfo` is standard and verified.

---

## 4. Conclusion

The LLM intuition module (`macro/slm_intuition.py`), macro blackout filter, systemd service definitions (`deploy/`), watchdog memory guard (`quant/hft/guard.py`), and CLI entrypoint (`run_relapse_scalper.py`) strictly fulfill all functional, architectural, and safety constraints specified in `ORIGINAL_REQUEST.md` and `PROJECT.md`.

All 44 automated tests across `test_hft_guard.py`, `test_gold_relapse_scalper.py`, and `test_scaling_simulation.py` pass with 100% success rate.

**Final Reviewer Verdict**: **APPROVE**

---

## 5. Verification Method

To independently reproduce and verify this review:
1. Run HFT watchdog unit tests:
   ```bash
   pytest tests/test_hft_guard.py -v
   ```
   *Expected: 14 passed.*
2. Run scalper CLI dry-run diagnostic:
   ```bash
   python3 run_relapse_scalper.py --paper --dry-run --initial-equity 65.0
   ```
   *Expected: Exit code 0, DRY_RUN_DIAGNOSTIC_PASSED in output.*
3. Run comprehensive scalper and simulation suites:
   ```bash
   pytest tests/test_gold_relapse_scalper.py tests/test_scaling_simulation.py -v
   ```
   *Expected: 30 passed.*
